//c++

class Solution {
public:
    int maxProduct(vector<int>& nums) {
      
      int n=nums.size()-1;
      int ans=0;
      sort(nums.begin(),nums.end());

      for(int i=n;i>0;--i){

          ans=((nums[n]-1)*(nums[n-1]-1));
      } 
        return ans;
        
    }
};
